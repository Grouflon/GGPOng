#include "Globals.h"

const sf::Vector2f gameSize(640.f, 480.f);
const sf::Vector2f playerSize(15.f, 100.f);
const sf::Vector2f ballSize(10.f, 10.f);
const float playerBorderOffset = 50.f;
const float playerSpeed = 6.f;
const float ballSpeed = 8.0f;
const float ballMaxAngle = 65.f;
const float playerMaxActionOnAngle = 40.f;
const int scoreLimit = 9;

sf::Font textFont;
sf::Font titleFont;
